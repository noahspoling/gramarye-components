# Gramarye Components

Library to separate out components in case systems need to access the same components. Components that reference a graphics implementation (like raylib) reference the component-functions library that have the forward declared components that use things defined in other libraries.

